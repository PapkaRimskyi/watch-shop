import React, { FC } from 'react';

import Button from './s-drop-filter-button';

const DropFilterButton: FC = () => (
  <Button type="reset">Сбросить фильтр</Button>
);

export default DropFilterButton;
